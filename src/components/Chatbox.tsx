import { Box } from '@mui/material';

const Chatbox = () => {
    return ( 
        <Box sx={{
            width: '70%',
            alignItems: 'center',
            justifyContent: 'center',
            margin: 'auto',
        }}>
            {/* Add UI here */}
            Hello Content
        </Box>
    );
}

export default Chatbox;